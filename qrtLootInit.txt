scn tryLootScript
;用于执行loot操作

Begin GameMode
	ref rActor
	set rActor to GetFirstRef; * 200 == actors
	while rActor != 0
		MessageEx "name= %n" rActor
		int tempType
		set tempType to GetType rActor
		MessageEx "type= %g" tempType
		if rActor.GetDead || rActor.GetDisabled || rActor.GetIsCreature
			; skip
		else
			rActor.PlayIdle MyDancingIdle
		endif
		set rActor to GetNextRef ; * make sure this is within the loop or CTD
	loop
End